import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  photo: {
    borderRadius: 20,
    width: '100%',
    height: 250,
  },
  date: {
    fontSize: 12,
    fontWeight: '700',
    marginTop: 10,
  },
});
